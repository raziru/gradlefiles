task downloadModelFile(type: Download) {
    src 'https://storage.googleapis.com/tensorflow_mine/mymodelLSTM.tflite'
    dest project.ext.ASSET_DIR + '/text_classification.tflite'
    overwrite true
}

preBuild.dependsOn downloadModelFile